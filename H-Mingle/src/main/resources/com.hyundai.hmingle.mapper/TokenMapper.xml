<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hyundai.hmingle.mapper.TokenMapper">

    <insert id="save">
        insert into token (member_id, access_token, refresh_token)
        values (2, 'accessToken', 'refreshToken')
    </insert>

    <select id="findByMemberId" resultType="com.hyundai.hmingle.domain.member.Token">
        select id, member_id
        from token
        where member_id = 2;
    </select>

    <update id="update">
        update token
        set access_token = 'access_token2',
            refresh_token = 'refresh_token2'
        where member_id = 2;
    </update>
</mapper>
