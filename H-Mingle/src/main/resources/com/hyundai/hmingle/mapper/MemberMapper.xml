<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hyundai.hmingle.mapper.MemberMapper">
    <insert id="save" parameterType="member" >
        insert into member(id, email, nickname, introduction, image_url)
        values (seq_memeber_id.nextval, #{email}, #{nickname}, #{introduction}, #{imageUrl})
    </insert>

    <select id="findByEmail" parameterType="string" resultType="member">
        select id, email, nickname, introduction, image_url
        from member
        where email = #{email}
    </select>

	<select id="getMember">
		SELECT id, email, nickname, introduction, image_url
		FROM member
		WHERE id=#{memberId}
	</select>

</mapper>

