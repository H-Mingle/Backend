<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hyundai.hmingle.mapper.TokenMapper">

    <insert id="save" parameterType="com.hyundai.hmingle.domain.member.Token" >
        insert into token (id, member_id, access_token, refresh_token)
        values (seq_token_id.nextval, #{member.id}, #{accessToken}, #{refreshToken})
    </insert>

    <update id="update" parameterType="com.hyundai.hmingle.domain.member.Token" >
        update token
        set access_token = #{accessToken},
            refresh_token = #{refreshToken}
        where member_id = #{member.id}
    </update>

    <select id="findByMemberId" parameterType="long" resultType="com.hyundai.hmingle.domain.member.Token">
        select id, member_id, access_token, refresh_token
        from token
        where member_id = #{memberId}
    </select>
</mapper>